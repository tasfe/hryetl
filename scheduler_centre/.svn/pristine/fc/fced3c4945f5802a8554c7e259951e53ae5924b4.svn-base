Ext.define('banggo.view.menu.MenuView', {
			extend : 'Ext.tree.Panel',
			alias : 'widget.menuView',
			collapsible : true,
			autoHeight : true,
			autoScroll : true,
			useArrows: true, //使用箭头
			rootVisible : true,// 是否隐藏root
			animate : true, // 以动画形式伸展,收缩子节点
			store : 'menu.MenuStore',
			displayField:'text',
			initComponent : function() {
				var me = this;
				me.callParent(arguments);
			}
		});