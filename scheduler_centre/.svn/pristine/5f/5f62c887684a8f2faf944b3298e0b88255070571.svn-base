/**
 * kettle更新时间
 */
Ext.define('banggo.view.content.UpdateKettleDatePanel', {
			extend : 'Ext.panel.Panel',
			alias : 'widget.updateKettleDatePanel',//别名
			layout : 'border',
			requires : ['banggo.view.taskManager.UpdateKettleDateGrid'],
			items : [{
				//位置
				region: "center",
				//分隔
				split: true,
				collapseMode: "mini",
				xtype:"updateKettleDateGrid"
			}],
			region: "center",
			initComponent: function () {
				this.callParent(arguments);
			}
		});